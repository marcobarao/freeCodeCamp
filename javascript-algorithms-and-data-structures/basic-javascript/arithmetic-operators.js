// Add two numbers
var sum = 10 + 10;

// Substract one number from another
var difference = 45 - 33;

// Multiply two numbers
var product = 8 * 10;

// Divide one number by another
var quotient = 66 / 33;

// Increment a number
var myVar = 87;
myVar++; // myVar = 88

// Decrement a number
myVar--; // myVar = 87

// Create a decimal number
var myDecimal = 5.7;

// Multiply two decimal numbers
var product = 2.0 * 2.5;

// Divide two decimal numbers
var quotient = 4.4 / 2.0;

// Finding a reminder
var remainder = 11 % 3;

// Compound assignment with augment addition
var a = 3;
var b = 17;
var c = 12;

a += 12;
b += 9;
c += 7;

// Compound assignment with augment substraction
a -= 6;
b -= 15;
c -= 1;

// Compound assignment with augment multiplication
a *= 5;
b *= 3;
c *= 10;

// Compound assignment with augment division
a /= 12;
b /= 4;
c /= 11;
